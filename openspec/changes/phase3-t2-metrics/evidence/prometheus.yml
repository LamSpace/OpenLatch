# T2 联调用最小 Prometheus 配置（详设 §7 T2"抓取联调记录在案"）
# 用法见 ../t2-acceptance-evidence.md
global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: openlatch
    static_configs:
      - targets: ['host.docker.internal:9412']
