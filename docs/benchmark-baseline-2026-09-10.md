# OpenLatch Phase 3 基准基线

生成：2026-09-10　来源：`BenchmarkMain`（design D5 手写 harness；harness 标题为静态字符串，本文件按阶段修订）

## 环境

| 项 | 值 |
|---|---|
| OS | Linux 7.0.0-31-generic |
| CPU | 8 核 |
| JDK | 25.0.3 |
| 服务器 | 进程内内嵌，默认配置（lease 30s / tick 500ms / 临时端口） |
| 客户端 | 单实例（连接多路复用），1 EventLoop 线程 |
| 采样 | 热身 4s；3 批 × 5s，吞吐取批中位；延迟为蓄水池样本（每线程 32768），分位数按批计算后取中位 |

## 指标

| 场景 | ops/s（中位） | 往返/授予延迟 P50 (ms) | P99 (ms) |
|---|---|---|---|
| 无竞争 tryLock 往返 | 11788 | 0.04 | 0.09 |
| 16 线程竞争 lock() | 11697 | 1.20 | 1.93 |
| 64 线程竞争 lock() | 11549 | 5.18 | 8.93 |

> 竞争场景延迟列为**授予延迟**（发起到授予，含排队）。本基线仅作防退化参考，不作发布门槛（详设 §10.5）。

## 与 Phase 1 基线对比（2026-08-29 → 2026-09-10）

| 场景 | ops/s | Δ | P50 (ms) | Δ | P99 (ms) | Δ |
|---|---|---|---|---|---|---|
| 无竞争 tryLock 往返 | 11272 → 11788 | +4.6% | 0.04 → 0.04 | ±0% | 0.10 → 0.09 | −10.0% |
| 16 线程竞争 lock() | 11424 → 11697 | +2.4% | 1.22 → 1.20 | −1.6% | 2.02 → 1.93 | −4.5% |
| 64 线程竞争 lock() | 11257 → 11549 | +2.6% | 5.30 → 5.18 | −2.3% | 8.68 → 8.93 | +2.9% |

判定（DoD §5.4-5 口径：退化 > 20% 需说明原因）：**无场景退化 > 20%**。吞吐全场景小幅上浮（+2.4%～+4.6%），延迟波动在 ±10% 内，属运行噪声量级（两环境仅内核小版本差异：7.0.0-30 → 7.0.0-31）。Phase 3 新增的扩展锁类型（Semaphore/Latch/FAIR）与 TLS/认证均为默认关闭路径，不进入本基准场景（无竞争与竞争 `lock()` 走既有互斥锁热路径），故无退化符合预期。
